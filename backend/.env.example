# Copy to `.env` in this folder (never commit `.env`).
# Optional — Limitless Exchange prediction-markets context on banking-sector stock detail.
# Create a key at https://limitless.exchange → profile → API keys.
LIMITLESS_API_KEY=
# LIMITLESS_API_BASE=https://api.limitless.exchange

# X/Twitter RSS via Nitter (public rsshub.app/twitter is broken — 404)
# NITTER_INSTANCES=https://nitter.net,https://nitter.cz
